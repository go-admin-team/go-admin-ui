# just a flag
ENV = 'production'

# base api
VUE_APP_BASE_API = ''

# Suppress Sass deprecation warnings
SASS_SILENCE_DEPRECATIONS = 'legacy-js-api'

# Google Analytics measurement id (G-XXXXXXXXXX).
# Empty means the tag is not injected at all: no request to googletagmanager.com,
# nothing in the built index.html. Intranet and offline deployments need it that
# way, so it ships empty here -- the demo deployment sets it in build.yml.
VUE_APP_GA_ID = ''

# ICP filing number shown in the login footer. Left empty on purpose --
# a filing number belongs to whoever operates the deployment.
VUE_APP_ICP = ''

# Fix this deployment to one language, e.g. 'zh-CN' or 'en-US'.
# Empty -- how every .env here ships -- lets each visitor decide: the language
# they picked, else their browser's, else Chinese. Setting it pins everyone to
# that language and removes the switcher, which is what an intranet running in a
# single language wants. A value that is not a language this build ships fails
# the build; it is not ignored.
VUE_APP_LOCALE = ''
